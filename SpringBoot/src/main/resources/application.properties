# Database connection (change to your actual user, password, and DB name)
spring.datasource.url=jdbc:mysql://localhost:3306/lms_db
spring.datasource.username=root
spring.datasource.password=chinmayadas

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# (Optional) Set server port
server.port=8080

# (Optional) Set time zone if needed for date handling
spring.datasource.hikari.connection-timezone=Asia/Kolkata
